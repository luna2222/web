<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Math 를 활용한 구구단 연습</title>
function randomInt(){ // 1~9 의 십진의 난수 리턴
Math.random()*9)+1;}
</head>
<body>
	<h3> Math 를 활용한 구구단 연습</h3>
	<hr>
	<script >
	//구구단 문제 발생
	var ques=randonInt()+"*"+randomInt();
	//
	var user =prompt(ques+" 값은 얼마 입니까?",0);
	if(user ==null) {
		document.write("구구단 연습을 종료");
	}
		else{
			var ans= eval(ques);//	
			if*ans==user)
			document.write("정답!");
			else
				document.write("아니오");
			document.write(ques+"="+ "<string>"+ans+</string>입니다<br>");
	}
	
	</script>
</body>
</html>